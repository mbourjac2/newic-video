<fieldset class="fieldset {{ hidden_class }}" data-input="{{ input_type }}">
  <div class="flex flex-col gap-4">
    <label for="{{ item.id }}">
      {% include "pages/estimate/form/question-popups.twig" %}
    </label>
    <input type="{{ input_type }}" id="{{ item.id }}" name="{{ item.id }}" class="border px-6 py-2 rounded-xl text-right" required/>
    {% include "pages/estimate/form/error-message.twig" %}
    {% include "pages/estimate/form/submit-field.twig" %}
  </div>
</fieldset>
